# ============================================================
#  .htaccess raiz — XAMPP / Apache
#  Redireciona todo o tráfego para public/
# ============================================================
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
